import styled from 'styled-components';
import { layout } from 'styled-system';

const Card = styled.div`
  margin: 10px;
  padding: 10px;
  height: 50px;
  background: green;
  ${layout}
`;

export default Card;
